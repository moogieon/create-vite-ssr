"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const utils_1 = require("./utils");
const { projectVersion } = utils_1.projectInfo;
const key = '__vite_plugin_ssr_version';
// @ts-ignore
const version = (globalThis[key] = globalThis[key] = projectVersion);
(0, utils_1.assertUsage)(version === projectVersion, `Multiple versions \`vite-pluging-ssr@${version}\` and \`vite-pluging-ssr@${projectVersion}\` loaded. Make sure to load the same version.`);
