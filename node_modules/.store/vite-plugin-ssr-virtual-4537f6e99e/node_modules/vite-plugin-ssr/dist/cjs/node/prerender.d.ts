import './page-files/setup';
export { prerender };
/**
 * Render your pages (e.g. for deploying to a static host).
 * @param partial Allow only a subset of pages to be pre-rendered.
 * @param root The root directory of your project (where `vite.config.js` live) (default: `process.cwd()`).
 * @param outDir The build directory of your project (default: `dist`).
 */
declare function prerender({ onPagePrerender, pageContextInit, configFile, ...deprecatedOptions }?: {
    onPagePrerender?: Function;
    pageContextInit?: Record<string, unknown>;
    configFile?: string;
    /** @deprecated */
    partial?: boolean;
    /** @deprecated */
    noExtraDir?: boolean;
    /** @deprecated */
    root?: string;
    /** @deprecated */
    outDir?: string;
    /** @deprecated */
    base?: string;
    /** @deprecated */
    parallel?: number;
}): Promise<void>;
