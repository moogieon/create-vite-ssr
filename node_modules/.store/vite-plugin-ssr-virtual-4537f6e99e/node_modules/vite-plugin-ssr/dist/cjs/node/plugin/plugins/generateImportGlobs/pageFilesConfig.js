"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getPageFilesConfig = void 0;
const utils_1 = require("../../../utils");
const utils_2 = require("../../utils");
function getPageFilesConfig(vitePluginSsrConfig) {
    const pageFilesConfig = {
        include: [],
    };
    if (vitePluginSsrConfig.pageFiles === undefined) {
        return pageFilesConfig;
    }
    (0, utils_2.assertUsage)((0, utils_1.hasProp)(vitePluginSsrConfig, 'pageFiles', 'object'), '[vite.config.js][`ssr({ pageFiles })`] `pageFiles` should be an object');
    const { pageFiles } = vitePluginSsrConfig;
    if (pageFiles.include !== undefined) {
        (0, utils_2.assertUsage)((0, utils_1.hasProp)(pageFiles, 'include', 'string[]'), '[vite.config.js][`ssr({ pageFiles: { include } })`] `include` should be a list of strings');
        pageFilesConfig.include = pageFiles.include;
    }
    return pageFilesConfig;
}
exports.getPageFilesConfig = getPageFilesConfig;
