export { isSSR_config };
export { isSSR_options };
declare function isSSR_config(config: {
    build?: {
        ssr?: boolean | string;
    };
}): boolean;
declare type Options = undefined | boolean | {
    ssr?: boolean;
};
declare function isSSR_options(options: Options): boolean;
