import React from 'react';
import { RelayEnvironmentProvider } from 'react-relay';
import { useRouteManager } from './routeManager';
import { PageContextProvider } from './usePageContext';
// Page root component
export const PageShell = ({ pageContext, relayEnvironment, routeManager, }) => {
    const PageLayout = pageContext.exports?.PageLayout ?? Passthrough;
    const [CurrentPage, queryRef, routeTransitioning] = useRouteManager(routeManager);
    return (React.createElement(React.StrictMode, null,
        React.createElement(RelayEnvironmentProvider, { environment: relayEnvironment },
            React.createElement(PageContextProvider, { pageContext: pageContext },
                React.createElement(PageLayout, { routeTransitioning: routeTransitioning }, CurrentPage && React.createElement(CurrentPage, { queryRef: queryRef }))))));
};
const Passthrough = ({ children }) => React.createElement(React.Fragment, null, children);
